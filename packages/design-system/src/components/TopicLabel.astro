---
export interface Props {
  href?: string;
  label: string;
}

const { href, label } = Astro.props;
---

{
  href ? (
    <a class="ds-topic-label" href={href}>
      {label}
    </a>
  ) : (
    <span class="ds-topic-label">{label}</span>
  )
}
