{
  "name": "@temis/tool-pipeline",
  "version": "0.1.0",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail",
    "types:generate": "wrangler types src/worker-configuration.d.ts",
    "type-check": "pnpm run types:generate && tsc --noEmit"
  },
  "dependencies": {
    "@temis/tool-github-data": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.61.1"
  }
}
