---
import PageHero from "@temis/design-system/components/PageHero.astro";
import SiteLayout from "../components/SiteLayout.astro";
import { entryDescription, entryTitle, getPageBySlug } from "../lib/emdash";
import { portableTextToHtml } from "../lib/portable-text";

const page = await getPageBySlug("about");
const title = page ? entryTitle(page) : "About";
const description = page
  ? entryDescription(page)
  : "TEMIS is a public map of a technical commons.";
---

<SiteLayout title={title} description={description}>
  <PageHero eyebrow="About" title={title} description={description} />

  {
    page?.content && (
      <article class="ds-prose" set:html={portableTextToHtml(page.content)} />
    )
  }
</SiteLayout>
